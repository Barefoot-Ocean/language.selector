% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_icons.R
\name{generate_icon_css}
\alias{generate_icon_css}
\title{Generate Icon CSS from Images}
\usage{
generate_icon_css(
  images_dir,
  output_dir,
  prefix = "language-flag-",
  css_filename = "custom-languages.css",
  width = 60,
  height = 60,
  mapping = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{images_dir}{Path to folder containing source images (PNG, JPG, SVG)}

\item{output_dir}{Path where CSS and images will be saved (e.g., "www/custom_flags")}

\item{prefix}{CSS class prefix (default: "language-flag-")}

\item{css_filename}{Name of generated CSS file (default: "custom-languages.css")}

\item{width}{Icon width in pixels (default: 60)}

\item{height}{Icon height in pixels (default: 60)}

\item{mapping}{Optional named list mapping filenames to class codes
(e.g., \code{list("germany.png" = "de")}). If NULL, uses filename as code.}

\item{overwrite}{Overwrite existing files (default: FALSE)}
}
\value{
Invisibly returns list with generated CSS path and icon names
}
\description{
Development helper that takes a folder of images and generates CSS file
with icon classes. Images are copied to output folder and CSS references them.
Use this to add custom language flags beyond the built-in ones.
}
\examples{
\dontrun{
# Add custom language flags
generate_icon_css(
  images_dir = "raw_images/flags",
  output_dir = "www/custom_flags",
  prefix = "language-flag-",
  mapping = list(
    "germany.png" = "de",
    "italy.png" = "it"
  )
)

# Then in your app, include the CSS:
# tags$link(rel = "stylesheet", href = "custom_flags/custom-languages.css")
}

}
